{% extends "layout.twig" %}

{% block content %}

    <h2>Users</h2>
    <a class="btn btn-primary" href="{{ "admin/users/create" | url }}">New User</a>
    <table class="table">
        <caption>Table Users</caption>
        <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.name }}</td>
                <td>{{ user.email }}</td>
                <td><a class="btn btn-warning" href="{{ "admin/post/update" | url }}">Edit</a></td>
                <td><a class="btn btn-danger" href="<?php echo BASE_URL;?>admin/posts/delete?id=<?php echo $blogPost['id'] ?>">Delete</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}